import './styles.css'

const Navbar = () => {
  return (
    <nav className="bg-primary nav-container">
      <div>
        <h4>Github API</h4>
      </div>
    </nav>
  );
};

export default Navbar;
